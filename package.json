{
  "name": "costco-exec-news-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently --names SERVER,CLIENT --prefix-colors blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently --names SERVER,CLIENT --prefix-colors blue,green \"npm start --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
